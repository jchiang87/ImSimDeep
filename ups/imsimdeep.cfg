# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required" : ["utils", "afw"],
    "buildRequired" : ["swig"],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[],
    hasDoxygenInclude=False,
    hasSwigFiles=True,
)
